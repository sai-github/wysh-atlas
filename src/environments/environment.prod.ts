import { firebaseConfig } from '../environments/firebase-config';

export const environment = {
  production: true,
  firebase: firebaseConfig,
  wyshAtlasFnURL:
    'https://us-central1-wysh-atlas.cloudfunctions.net/',
};
